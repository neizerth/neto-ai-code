// TODO: Реализовать хук useRegister с использованием useMutation из React Query.
// Мутация должна вызывать API-функцию registerUser (её нужно добавить в api/auth).
// Экспортировать результат useMutation для использования в RegistrationForm.

export function useRegister() {
  // Заглушка: заменить на useMutation + registerUser
  throw new Error(
    "useRegister: не реализовано. Добавьте registerUser в api/auth и useMutation здесь.",
  );
}
